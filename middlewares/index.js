const validateBody = require('./validateBody')
const isValidIdFunc = require('./isValidId')
const validateToken = require('./validateToken')

module.exports = {
  validateBody,
  isValidIdFunc,
  validateToken
}